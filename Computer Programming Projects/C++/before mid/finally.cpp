#include <iostream>

using namespace std;
int main()
{
	string name[5];
	int mark[4];
	for (int i = 0; i<=4;i++ )
	{
		cout<<"Name of instructor \t"<<name[i];
		cin>>name[i];
		cout<<"The instructor arrives on time"<<"\t";
		cin>>mark[0];
		cout<<"The instructor leaves on time"<<"\t";
		cin>>mark[1];
		cout<<"The instructor is prepared for each class"<<"\t";
		cin>>mark[2];
		cout<<"The instructor communicates the subject matter effectively"<<"\t";
		cin>>mark[3];
		cout<<""
	}
}
